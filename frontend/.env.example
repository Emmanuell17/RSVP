# Production / Vercel: public URL of your Node backend (Render, Railway, Fly.io, etc.)
# No trailing slash. After setting in Vercel → Environment Variables, redeploy the frontend.
# REACT_APP_API_URL=https://your-api.example.com
