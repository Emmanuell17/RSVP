{
  "name": "rsvp-website",
  "version": "1.0.0",
  "private": true,
  "description": "Heroku entry: installs backend deps then runs backend/server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "npm ci --prefix backend",
    "start": "node backend/server.js"
  }
}
